# Challenge data (large / provided separately, do not redistribute)
challenge_data/
Data_T0/
Data_T1/
Development_Labels/
Submission_Template.csv

# Derived demo data (regenerate with: python export_case_data.py)
# Kept out of the repo because it contains challenge-derived evidence trails
# and is large (~30 MB).
demo_data/

# Self-contained HTML demo (regenerate with: python export_html.py).
# Embeds challenge-derived evidence for the curated + worklist cases, so it's
# kept out of the repo for the same reason as demo_data/. The generator
# (export_html.py + html_template.html) IS tracked, so it can be rebuilt.
dmv_demo.html

# Secrets / credentials
.env
*.key
*_key*.json
service-account*.json
gcp-*.json
credentials.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
